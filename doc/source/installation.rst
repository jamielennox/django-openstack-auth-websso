============
Installation
============

At the command line::

    $ pip install django-openstack-auth-websso

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-openstack-auth-websso
    $ pip install django-openstack-auth-websso
